@model EvilLyo.Models.Story
@{
    ViewData["Title"] = Model.Title;
    var msg = Model.Content;
}

<h2>@Model.Title</h2>

<div>
    <dl class="dl-horizontal">
        <dt>
            @Model.Title
        </dt>
        <dd>
            @Model.Title
        </dd>
    </dl>
</div>

@Html.Raw(msg)

<div>
    <a asp-action="Index">回到主页</a>
</div>